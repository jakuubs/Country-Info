import { CountryCardDirective } from './country-card.directive';

describe('CountryCardDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryCardDirective();
    expect(directive).toBeTruthy();
  });
});
