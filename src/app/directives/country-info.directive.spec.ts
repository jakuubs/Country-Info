import { CountryInfoDirective } from './country-info.directive';

describe('CountryInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryInfoDirective();
    expect(directive).toBeTruthy();
  });
});
